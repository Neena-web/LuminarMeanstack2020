for(let i=0;i<10;i++)
{
    console.log(i)
}
console.log("i"+i);
//let=if we use let that variable is only useable in that scope.
//suppose we use it in a for loop that variable is used inside that varibale .otherwise it got error
